const InitialState = {Finance:['AX'],
	Technology: ['BX'],
	Industries: ['CV'],
};

export default (state = InitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
