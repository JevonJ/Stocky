export const calculateStocks = (roomName) => {
  return dispatch => new Promise((resolve, reject) => {
    // dispatch({
    //   type: REMOVE_ROOM,
    //   payload: roomName,
    // });
    resolve();
  });
};